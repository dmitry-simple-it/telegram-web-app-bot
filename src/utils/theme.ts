import { WebApp } from './tgWebApp';

export const themeParams = {
  bgColor: WebApp.themeParams.bg_color || '#272626',
  textColor: WebApp.themeParams.text_color || '#fff',
  hintColor: WebApp.themeParams.hint_color || '#000',
  linkColor: WebApp.themeParams.link_color || '#3cbcd5',
  buttonColor: WebApp.themeParams.button_color || '#3dbdd6',
  buttonTextColor: WebApp.themeParams.button_text_color || '#fff',
  secondaryBgColor: WebApp.themeParams.secondary_bg_color || '#181819',
};
