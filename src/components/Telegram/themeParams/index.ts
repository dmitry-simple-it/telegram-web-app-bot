import { tgWebApp } from '../core/tgWebApp';

export const themeParams = {
  bgColor: tgWebApp.themeParams.bg_color || '#272626',
  textColor: tgWebApp.themeParams.text_color || '#fff',
  hintColor: tgWebApp.themeParams.hint_color || '#000',
  linkColor: tgWebApp.themeParams.link_color || '#3cbcd5',
  buttonColor: tgWebApp.themeParams.button_color || '#3dbdd6',
  buttonTextColor: tgWebApp.themeParams.button_text_color || '#fff',
  secondaryBgColor: tgWebApp.themeParams.secondary_bg_color || '#181819',
};
